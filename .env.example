# PDF Reader MCP - Environment Variables Example
# Copy this file to .env and fill in your values

# ==============================================
# OCR / Vision API Keys (Optional)
# ==============================================
# If set, these keys enable automatic OCR/Vision providers
# LLMs can call pdf_ocr_page/pdf_ocr_image without explicit provider config

# Mistral API Key (for Vision API and OCR API)
# Get your key from: https://console.mistral.ai/
MISTRAL_API_KEY=your-mistral-api-key-here

# ==============================================
# Path Security (Optional)
# ==============================================
# Configure which directories the server can access

# Base directory for resolving relative paths
# Default: process.cwd()
# PDF_BASE_DIR=/path/to/documents

# Allowed root directories (: delimited on Unix, ; on Windows)
# Only paths within these directories are accessible
# Default: process.cwd()
# PDF_ALLOWED_PATHS=/home/user/Documents:/mnt/pdfs:/tmp/scans

# Allow absolute paths outside allowlist (⚠️ UNSAFE - not recommended)
# Default: false
# PDF_ALLOW_UNSAFE_ABSOLUTE=true

# ==============================================
# Development / Debugging (Optional)
# ==============================================
# Enable detailed logging
# LOG_LEVEL=debug

# ==============================================
# Usage Examples
# ==============================================

# Example 1: Mistral OCR with path security
# MISTRAL_API_KEY=sk-xxx
# PDF_BASE_DIR=/home/user/Documents
# PDF_ALLOWED_PATHS=/home/user/Documents:/mnt/pdfs

# Example 2: Development (unsafe mode)
# MISTRAL_API_KEY=sk-xxx
# PDF_ALLOW_UNSAFE_ABSOLUTE=true

# Example 3: Multiple directories (Linux/macOS)
# MISTRAL_API_KEY=sk-xxx
# PDF_ALLOWED_PATHS=/home/user/docs:/mnt/pdfs:/tmp/scans

# Example 4: Multiple directories (Windows)
# MISTRAL_API_KEY=sk-xxx
# PDF_ALLOWED_PATHS=C:\Users\User\Documents;D:\PDFs;E:\Scans
